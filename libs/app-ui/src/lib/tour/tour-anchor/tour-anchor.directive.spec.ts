import { RenwuTourAnchorDirective } from './tour-anchor.directive';

describe('TourAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new RenwuTourAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
